@page
@model TravelGuideApp.Pages.Admin.Hotel.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hotel[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotel[0].Url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotel[0].nbEtoile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotel[0].IdAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotel[0].description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Hotel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nbEtoile)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
